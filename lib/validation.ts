import {z} from 'zod';
export const guestSchema=z.object({name:z.string().trim().max(100).default(''),discounts:z.array(z.enum(['standard','student','nhs','older','member'])).max(5),photography:z.boolean(),clothing:z.string().max(100),backupSize:z.string().max(30),colour:z.string().max(60),notes:z.string().max(500)});
export const bookingSchema=z.object({eventId:z.string().max(100),adults:z.number().int().min(1).max(20),children:z.number().int().min(0).max(20),guests:z.array(guestSchema).min(1).max(20),dietary:z.string().max(1500),allergies:z.string().max(1500),accessibility:z.string().max(1500),marketingConsent:z.boolean(),privatePhotographyConsent:z.boolean()}).refine(v=>v.guests.length===v.adults,'Please complete the details for each adult guest.');
export const contactSchema=z.object({name:z.string().trim().min(2).max(100),email:z.string().email().max(254),phone:z.string().max(40).default('')});
export const checkoutSchema=z.object({contact:contactSchema,delivery:z.enum(['collection','shipping']),address:z.string().max(1000).default(''),terms:z.literal(true),demo:z.boolean(),failure:z.boolean().default(false),giftCode:z.string().max(80).default(''),promoCode:z.string().max(80).default(''),idempotencyKey:z.string().uuid()});
